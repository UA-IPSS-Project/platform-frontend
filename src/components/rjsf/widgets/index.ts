import { TextWidget, TextareaWidget, EmailWidget, DateWidget, CheckboxWidget, SelectWidget, RadioWidget } from './BasicWidgets';
import { TableFieldWidget } from './TableFieldWidget';
import { FileFieldWidget } from './FileFieldWidget';

export const rjsfWidgets = {
  TextWidget,
  EmailWidget,
  DateWidget,
  TextareaWidget,
  CheckboxWidget,
  SelectWidget,
  RadioWidget,
  TableFieldWidget,
  tableField: TableFieldWidget,
  fileField: FileFieldWidget,
};
